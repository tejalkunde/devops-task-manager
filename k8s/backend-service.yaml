apiVersion: v1
kind: Service
metadata:
  name: task-backend
  namespace: task-manager
spec:
  selector:
    app: task-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP