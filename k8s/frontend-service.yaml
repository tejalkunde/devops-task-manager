apiVersion: v1
kind: Service
metadata:
  name: task-frontend
  namespace: task-manager
spec:
  selector:
    app: task-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP